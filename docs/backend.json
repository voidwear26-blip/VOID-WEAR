{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account with basic profile information within the VOID WEAR application. Does not store authentication credentials.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's unique email address, used for communication.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "createdAt",
        "updatedAt"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents an individual product available for sale in the 'VOID WEAR' app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product, including features and materials."
        },
        "price": {
          "type": "number",
          "description": "The current selling price of the product."
        },
        "currency": {
          "type": "string",
          "description": "The currency of the product price (e.g., 'USD', 'EUR')."
        },
        "imageUrls": {
          "type": "array",
          "description": "An array of URLs to product images, showcasing different views or details.",
          "items": {
            "type": "string"
          }
        },
        "category": {
          "type": "string",
          "description": "The primary category of the product (e.g., 'Tops', 'Bottoms', 'Accessories')."
        },
        "stockQuantity": {
          "type": "number",
          "description": "The number of units of this product currently available in stock."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the product was first added to the catalog.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the product information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "price",
        "currency",
        "imageUrls",
        "category",
        "stockQuantity",
        "createdAt",
        "updatedAt"
      ]
    },
    "Cart": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Cart",
      "type": "object",
      "description": "Represents a user's active shopping cart, containing selected products before checkout.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Cart entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this cart. (Relationship: User 1:N Cart)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the shopping cart was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the cart was last updated (e.g., items added, removed, or quantities adjusted).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "createdAt",
        "updatedAt"
      ]
    },
    "CartItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CartItem",
      "type": "object",
      "description": "Represents a single product entry within a user's shopping cart, specifying the product and its quantity.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CartItem entity."
        },
        "cartId": {
          "type": "string",
          "description": "Reference to the Cart this item belongs to. (Relationship: Cart 1:N CartItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the Product included in this cart item. (Relationship: Product 1:N CartItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product in this specific cart item."
        },
        "addedAt": {
          "type": "string",
          "description": "Timestamp indicating when this item was added to the cart.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "cartId",
        "productId",
        "quantity",
        "addedAt"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a confirmed customer purchase, containing overall transaction details and references to ordered items.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who placed this order. (Relationship: User 1:N Order)"
        },
        "orderDate": {
          "type": "string",
          "description": "The date and time when the order was placed and confirmed.",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number",
          "description": "The total monetary amount of the order, including taxes and shipping."
        },
        "currency": {
          "type": "string",
          "description": "The currency of the total order amount (e.g., 'USD', 'EUR')."
        },
        "status": {
          "type": "string",
          "description": "The current fulfillment status of the order (e.g., 'pending', 'processed', 'shipped', 'delivered', 'cancelled')."
        },
        "shippingAddress": {
          "type": "string",
          "description": "The full shipping address where the order will be delivered."
        },
        "billingAddress": {
          "type": "string",
          "description": "The full billing address associated with the payment method for the order."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the order record was initially created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the order record was last updated (e.g., status change).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "orderDate",
        "totalAmount",
        "currency",
        "status",
        "shippingAddress",
        "billingAddress",
        "createdAt",
        "updatedAt"
      ]
    },
    "OrderItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderItem",
      "type": "object",
      "description": "Represents a single product within a confirmed order, capturing its state (price, quantity) at the time of purchase.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the OrderItem entity."
        },
        "orderId": {
          "type": "string",
          "description": "Reference to the Order this item belongs to. (Relationship: Order 1:N OrderItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the Product that was purchased as part of this order item. (Relationship: Product 1:N OrderItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product included in this order item."
        },
        "priceAtOrder": {
          "type": "number",
          "description": "The unit price of the product at the exact moment the order was placed."
        },
        "currency": {
          "type": "string",
          "description": "The currency of the price at order (e.g., 'USD', 'EUR')."
        }
      },
      "required": [
        "id",
        "orderId",
        "productId",
        "quantity",
        "priceAtOrder",
        "currency"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles. Access is restricted to the owner matching `request.auth.uid`. Includes denormalized `userId` in the path for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, typically matching Firebase Authentication's `uid`."
            }
          ]
        }
      },
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores details of all products available in the store. This collection is publicly readable, while write access is restricted to administrators. No specific user-based denormalization needed as it's not user-owned.",
          "params": [
            {
              "name": "productId",
              "description": "The unique identifier for a product."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/carts/{cartId}",
        "definition": {
          "entityName": "Cart",
          "schema": {
            "$ref": "#/backend/entities/Cart"
          },
          "description": "Represents a user's active shopping cart. Nested under the user's document to enforce ownership. The `userId` in the path ensures authorization independence and facilitates secure list operations (QAPs).",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns this cart."
            },
            {
              "name": "cartId",
              "description": "The unique identifier for a specific shopping cart."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/carts/{cartId}/cartItems/{cartItemId}",
        "definition": {
          "entityName": "CartItem",
          "schema": {
            "$ref": "#/backend/entities/CartItem"
          },
          "description": "Represents individual items within a user's shopping cart. Nested under the specific cart and user for clear ownership. `userId` and `cartId` in the path provide authorization independence for list and item-level access.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent cart."
            },
            {
              "name": "cartId",
              "description": "The unique identifier of the parent cart."
            },
            {
              "name": "cartItemId",
              "description": "The unique identifier for a specific item within a cart."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Represents a user's confirmed purchase order. Nested under the user's document to strictly enforce ownership. `userId` in the path ensures authorization independence and supports QAPs for listing user-specific orders.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who placed this order."
            },
            {
              "name": "orderId",
              "description": "The unique identifier for a specific order."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/orders/{orderId}/orderItems/{orderItemId}",
        "definition": {
          "entityName": "OrderItem",
          "schema": {
            "$ref": "#/backend/entities/OrderItem"
          },
          "description": "Represents individual products included in a confirmed order. Nested under the specific order and user for explicit ownership. `userId` and `orderId` in the path guarantee authorization independence for all item-level operations.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent order."
            },
            {
              "name": "orderId",
              "description": "The unique identifier of the parent order."
            },
            {
              "name": "orderItemId",
              "description": "The unique identifier for a specific item within an order."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to be secure, scalable, and debuggable by strictly adhering to the core design principles and mandates. The primary strategy for authorization independence and queryable access patterns (QAPs) is a robust hierarchical path structure, especially for user-owned and nested data. For instance, all user-specific data (carts, orders, and their respective items) are nested under `/users/{userId}/...`. This inherently denormalizes the `userId` into the document path, making authorization rules extremely simple and atomic: a user can only access documents where `{userId}` in the path matches `request.auth.uid`. This eliminates the need for `get()` calls in security rules, as the ownership is self-evident from the path.\n\nPublicly accessible data, such as `Product` information, is placed in a top-level collection (`/products/{productId}`). This segregation ensures that public data has a distinct and homogeneous security posture (read-all, write-admin) from private user data, simplifying rule sets and preventing accidental data exposure or unauthorized modification.\n\nFor QAPs, the hierarchical structure directly supports efficient and secure queries. When a user queries their cart items at `/users/{userId}/carts/{cartId}/cartItems`, the query is implicitly filtered by the `userId` in the path, ensuring that users can only list their own items. The same applies to orders and order items. The `userId` and `cartId`/`orderId` fields within the respective documents serve as redundant references for data integrity and specific ad-hoc queries, but the path itself is the primary enforcement mechanism for access control. This design prevents situations where security rules act as filters (which is inefficient and prone to errors) by ensuring that any `list` operation implicitly respects the user's authorization scope."
  }
}