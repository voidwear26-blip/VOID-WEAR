
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // VOID WEAR // ABSOLUTE ACCESS OVERRIDE v26.0
    // Unrestricted access protocol for all collections to ensure 
    // administrative modules function without permission blocks.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
