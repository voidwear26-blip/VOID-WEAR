
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // UNRESTRICTED ACCESS FOR PROTOTYPING
    // Allowing all operations on all collections to ensure no "insufficient permissions" errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
